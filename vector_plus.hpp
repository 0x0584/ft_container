#ifndef VECTOR_PLUS
#define VECTOR_PLUS

#include "vector.hpp"

namespace ft {
template <typename T, typename Allocator>
class vector_plus : vector<T, Allocator> {
public:
  vector_plus() = {}
};
} // namespace ft

#endif // VECTOR_PLUS
